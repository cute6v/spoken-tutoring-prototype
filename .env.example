# Copy to .env and fill in your own values. Never commit .env.
#
# Used by tutor/adaptive_feedback.py (DeepSeek via OpenAI-compatible API).
# If unset, the prototype falls back to rule-based feedback automatically.

DEEPSEEK_API_KEY=your_deepseek_api_key_here
